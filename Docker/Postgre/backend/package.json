{
  "name": "cooksmart-api",
  "version": "1.0.0",
  "description": "API por capas de CookSmart (Postgres). Capas: routes -> controllers -> services -> repositories -> db.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^14.3.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.12.0"
  }
}
